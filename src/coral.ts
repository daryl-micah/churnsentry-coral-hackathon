export type CoralQueryResult = {
  raw: unknown;
};

export async function runCoralQuery(_customerId: string): Promise<CoralQueryResult> {
  // TODO: implement Coral query helpers
  return { raw: null };
}
